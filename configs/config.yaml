server:
  port: ":8080"
  
database:
  dsn: "${DATABASE_DSN}"
  
auth:
  access_token_secret: "${ACCESS_TOKEN_SECRET}"
  
logging:
  level: "info"
  format: "json"

kafka:
  brokers: ["localhost:9092"]
  teamActivityTopic: "team.activity"